.venv

.idea

.env

banned_users.txt

music_bot.db
music_bot.db-shm
music_bot.db-wal

__pycache__

.cache

audio_cache

.claude/

# Activity frontend build artifacts
activity-frontend/node_modules/
activity-frontend/.next/

# out/ is deliberately committed so hosts without Node can serve the Activity
# UI. Rebuild and commit it after any UI change or they keep the old bundle.